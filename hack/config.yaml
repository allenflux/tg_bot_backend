
# CLI tool, only in development environment.
# https://goframe.org/docs/cli
gfcli:
  gen:
    dao:
      - link: "mysql:root:Baizhe@2024@tcp(127.0.0.1:33062)/tg_bot?charset=utf8mb4&parseTime=true&loc=Local"
        descriptionTag: true

  docker:
    build: "-a amd64 -s linux -p temp -ew"
    tagPrefixes:
      - my.image.pub/my-app
